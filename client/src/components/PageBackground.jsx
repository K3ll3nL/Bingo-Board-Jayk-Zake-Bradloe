// Old target-style tiled background (bb-smart.png) removed.
// Kept as a no-op so existing `<PageBackground />` usages don't need changing.
const PageBackground = () => null;

export default PageBackground;
